name: Deploy to server

on:
  - push

jobs:
  set-up:
    runs-on: ubuntu-latest

    steps:
      - name: Hola mundo
        run: echo "hola mundo"
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Hola mundo
        run: echo "hola mundo"
