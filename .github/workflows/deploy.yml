name: Deploy to server

on:
  - push

jobs:
  set-up:
    runs-on: ubuntu-latest

    steps:
      - name: Hola mundo
        run: echo "hola mundo"

  deploy:
    needs: set-up
    runs-on: ubuntu-latest

    steps:
      - name: test
        run: echo "test"
